.header { background: $head-bg; }

.header-inner { position: relative; }

.jumbotron{
  width: 100%;
  height: 23vh;
  position: absolute;
  background: url(/images/banner.jpg) center #000;
  +tablet-mobile() {
    hide()
  }
}

@import "site-meta";
@import "site-nav";
@import "menu";
